<div class="accordion-toggle" id="<%= menu.id %>">
  <%= menu.name %>
  <span class="edit-menu">
    <%= link_to t('ubiquo.edit'), ubiquo.edit_menu_path(menu) %>
  </span>
</div>
<div class="accordion-content">
  <div class="accordion-scroll">
    <%= print_menu_structure(menu) %>
  </div>
</div>
